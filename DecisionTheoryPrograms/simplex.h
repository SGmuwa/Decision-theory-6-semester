#pragma once

/*
Возвращает: код ошибки.
1 - Функция не реализована.
*/
int Simplex_run(int f(unsigned char length, const double * x, double * output), char isNeedMax, double accuracy, double * output) {
	return 1; // not impl
}
